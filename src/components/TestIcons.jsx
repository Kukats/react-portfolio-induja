import * as Icons from "react-icons/si";

export default function TestIcons() {
  console.log(Icons); // View all available SI icons in console

  return (
    <div style={{ color: "white", padding: "20px" }}>
      <h2>Testing Available Icons</h2>
      <p>Open browser Console → you will see the list of exports.</p>
    </div>
  );
}
